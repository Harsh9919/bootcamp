#include <stdio.h>
int countdigits(int n){
    if(n==0){
        return 0;
    }
    else{
        return 1+countdigits(n/10);
    }
}
int main() {
    // Write C code here
    int n;
    printf("enter the valus");
    scanf("%d",&n);
    printf("%d",countdigits(n));

    return 0;
}
